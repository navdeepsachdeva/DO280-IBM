kind: Deployment
metadata:
  name: dev-app
  labels:
    app: dev
spec:
  replicas: 1
  template:
    metadata:
      name: jay-pod
    spec:
      containers:
        - name: web
          ports:
            - containerPort: 8443
              protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: dev
  name: dev-app
spec:
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  selector:
    app: dev
